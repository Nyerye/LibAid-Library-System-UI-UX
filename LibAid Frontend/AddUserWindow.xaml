<Window x:Class="LibAid_Frontend.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User"
        Height="260"
        Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="First Name:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="FirstNameBox" Grid.Row="1" Height="30" Margin="0,0,0,15"/>

        <TextBlock Text="Last Name:" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="LastNameBox" Grid.Row="3" Height="30" Margin="0,0,0,15"/>

        <Button Content="Add User"
                Grid.Row="4"
                Height="35"
                Width="100"
                HorizontalAlignment="Center"
                Click="AddUser_Click"/>
    </Grid>
</Window>
