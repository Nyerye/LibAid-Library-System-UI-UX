<Window x:Class="LibAid_Frontend.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User" Height="240" Width="350"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#fafafa" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="First Name:" VerticalAlignment="Center" Margin="0 5 10 5"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="FirstNameBox" Margin="0 5" Height="25"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Last Name:" VerticalAlignment="Center" Margin="0 5 10 5"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="LastNameBox" Margin="0 5" Height="25"/>

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="Add User" Click="AddUser_Click"
                Margin="0 10 0 0" Height="35" Width="120" HorizontalAlignment="Center"/>
    </Grid>
</Window>
