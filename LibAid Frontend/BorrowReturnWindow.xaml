<Window x:Class="LibAid_Frontend.BorrowReturnWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Borrow / Return Book" Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0 0 0 10">
            <TextBlock Text="Book Title"/>
            <TextBox x:Name="TitleBox" Margin="0 0 0 10"/>

            <TextBlock Text="User ID"/>
            <TextBox x:Name="UserIdBox" Margin="0 0 0 10"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button Content="Borrow" Click="Borrow_Click" Margin="5"/>
            <Button Content="Return" Click="Return_Click" Margin="5"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="2" Text="" Margin="0 10 0 0" TextWrapping="Wrap"/>
    </Grid>
</Window>
